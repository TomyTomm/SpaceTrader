package com.amath.spacetrader.model;

public class PlanetInteractor extends Interactor {
    protected PlanetInteractor(Repository repo) {
        super(repo);
    }

    //Access the Model singleton access with:
    //Model model = Model.getInstance();

}
